<h1><%=_('Editing car')%></h1>

<% form_for(@car) do |f| %>
  <%= f.error_messages %>
  <%= render 'form', :f=>f%>
  <p>
    <%= f.submit _("Update") %>
  </p>
<% end %>

<%= link_to _('Show'), @car %> |
<%= link_to _('Back'), cars_path %>
