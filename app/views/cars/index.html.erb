<h1>Listing cars</h1>
<%x='this is a dynamic translation which was found thorugh gettext_test_log!'%>
<%=_(x)%>

<table>
  <tr>
  </tr>

<% for car in @cars %>
  <tr>
    <td><%= car.model %></td>
    <td><%= car.wheels_count %></td>
    <td><%= link_to 'Show', car %></td>
    <td><%= link_to 'Edit', edit_car_path(car) %></td>
    <td><%= link_to 'Destroy', car, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New car', new_car_path %>
