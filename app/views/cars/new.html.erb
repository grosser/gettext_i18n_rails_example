<h1><%=_('New car')%></h1>

<%= form_for(@car) do |f| %>
  <%= f.error_messages %>
  <%= render 'form', :f=>f%>
  <p>
    <%= f.submit _("Create") %>
  </p>
<% end %>

<%= link_to _('Back'), cars_path %>
